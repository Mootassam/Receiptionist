class CheckTheme {
  static checkTheme(theme) {
    if (theme === "phonepe1") {
      return true;
    }

    if (theme === "phonepe2") {
      return true;
    }

    if (theme === "phonepe3") {
      return true;
    }

    if (theme === "bharat") {
      return true;
    }

    if (theme === "sbimessage") {
      return true;
    }

    return false;
  }
}

export default CheckTheme;
